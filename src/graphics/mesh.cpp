#include "mesh.hpp"

namespace Hiruki {
	namespace Graphics {
		Mesh Mesh::loadFromFile(std::string filename) {
			Mesh mesh;

			return mesh;
		}

		Mesh Mesh::defaultCube() {
			Mesh mesh;
			return mesh;
		}
	}
}
